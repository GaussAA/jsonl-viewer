# Checklist

- [x] 设计 token 层已就位：`:root` 含间距/圆角/字号/过渡/阴影刻度，且全部派生自 `--vscode-*`，无写死色值。
- [x] 滚动条样式、`:focus-visible` 焦点环、`prefers-reduced-motion` 降级均已实现。
- [x] 顶栏已分层 / 分组：文件名+状态 与 统计芯片+搜索+工具 分区清晰，窄窗口不拥挤。
- [x] 统计项为图标化芯片；搜索框含清除按钮与命中计数；筛选/字段按钮有明确 active 态。
- [x] `toolbar.ts` 对外接口签名保持不变，`webviewEntry.ts` 调用处无协议/调用逻辑改动。
- [x] 左侧卡片：行号左侧等宽导轨、字段主次层级、状态类字段语义徽标着色均已实现。
- [x] 卡片选中态（强调边框+底色）、悬停态、错误卡、加载骨架样式与过渡均已实现。
- [x] 右侧 JSON 树：缩进引导线、行悬停/选中强调、类型配色微调、空/加载/错误态均已完成。
- [x] 工具按钮图标化、面包屑可读性提升；`detailLogic.ts` 树状态/路径逻辑未改动。
- [x] 横幅、主体布局、`max-width:700px` 堆叠断点、整体间距/圆角/阴影统一收尾完成。
- [x] `pnpm typecheck` 通过。
- [x] `pnpm test` 全绿（91 项测试全部通过，纯逻辑测试不受视觉改动影响）。
- [x] 深浅主题切换下配色随 `--vscode-*` 自适应、对比清晰。