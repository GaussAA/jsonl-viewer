# pnpm workspace / settings (pnpm 10+ reads settings here, see https://pnpm.io/settings)
packages:
  - "."

# Allow esbuild to run its postinstall (it ships native platform binaries).
onlyBuiltDependencies:
  - esbuild
allowBuilds:
  '@vscode/vsce-sign': true
  esbuild: true